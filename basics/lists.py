lista = [1, 2 , 3]
# Copy lists
listaDos = lista.copy()
# Add elements to a list
lista.append(4)
# Search for an element in lists
lista.count(3)
# input length of the list
len(lista)
# Remove last item in the list
lista.pop()
# Remove a specific element by its value in the list
lista.remove("element")
# this method returns a reverse list
lista.reverse()
# Order a list ONLY if it has the same type of data
list.sort()
print(lista[1])
